# Moodify Backend Server — Environment Variables
# Set these in Render Dashboard → Service → Environment

# Port the server listens on (Render sets PORT automatically, but you can override)
MOODIFY_SERVER_PORT=8787

# Set to "production" on Render to disable verbose diagnostics logging
NODE_ENV=production
